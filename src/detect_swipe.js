


const detect_swipe = (e) => {
	// if(!start_posX && !start_posY){
	// 	start_posX = e.touches[0].clientX;
	// 	start_posY = e.touches[0].clientY;
	// 	console.log(start_posX, start_posY);
	// }
	window.scroll({
		top: '772px',
		behaviour: 'smooth',
	});
	console.log(e);
}




export default detect_swipe;
